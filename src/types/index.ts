export type folderType = {
  name: string;
  size: string;
};
export type filetype = {
  name: string;
  size?: string;
  type: string;
  date: string;
  icon: string;
  image?: string;
  iconBgColor: string;
};
